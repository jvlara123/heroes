import { UppercaseAllDirective } from './uppercase-all.directive';

describe('UppercaseAllDirective', () => {
  it('should create an instance', () => {
    const directive = new UppercaseAllDirective();
    expect(directive).toBeTruthy();
  });
});
