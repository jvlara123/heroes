export class TableAction {
    icon: string; 
    action: string;
    color: string
    constructor(icon: string, action: string, color: string){
        this.icon = icon;
        this.action = action;
        this.color = color;
    }
}